{
  "name": "pick-a-side",
  "version": "0.1.0",
  "private": true,
  "description": "Pick A Side static production preview.",
  "scripts": {
    "check": "node --check app.js",
    "start": "npx serve ."
  }
}
